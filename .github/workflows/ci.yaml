name: ci

on:
  push: 
    paths:
      - "main-list/**/*.json"
      - ".github/workflows/**/*.yaml"
    branches: # Do not run on push event in case of a non-main branch (PR event will trigger the CI if the branch is associated to an open PR)
      - main
  pull_request: 
    paths:
      - "main-list/**/*.json"
      - ".github/workflows/**/*.yaml"
  schedule:
    # Daily at 01:00 UTC
    - cron: "0 1 * * *"      
  workflow_dispatch:

jobs:
  check_and_generate:
    # Do not run on push event in the context of an open PR
    if: ${{ github.event_name != 'push' || github.event.pull_request == null }}
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: ❄️ Install Nix
        uses: cachix/install-nix-action@v31

      - name: ❄️ Install jq
        if: false   # 👈 disables this step as Jq appears to be already installed
        run: sudo apt-get update && sudo apt-get install -y jq  

      - name: ❄️ Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 0️⃣ Import bunch of metadata from Google sheets (Exa-DoST)
        run: |
          mkdir snapshot
          touch snapshot/test

      - name: 1️⃣ Validate input Json files
        run: |
          echo "Checking projects.json schema..."
          nix run nixpkgs#check-jsonschema -- \
            --schemafile ./main-list/projects-schema.json ./main-list/projects.json
          echo "Checking uniqueness of names..."
          jq -e '
            [.projects[].name] as $names 
            | $names
            | group_by(.) 
            | map(select(length > 1) | .[0]) as $dupes 
            | if ($dupes | length) > 0 then 
                ( "Error: duplicate project name(s) found: " + ($dupes | join(", ")) ) 
                | halt_error(1) 
              else 
                "All project names are unique" 
              end 
            ' ./main-list/projects.json
          echo "Checking mapping.json schema..."
          nix run nixpkgs#check-jsonschema -- \
            --schemafile ./main-list/mapping-schema.json ./main-list/mapping.json
           
      - name: 2️⃣ Regenerate catalog
        run: |
          # At the moment we set conservative mode in all cases so that the pipeline will fail if something goes wrong
          OPTS="--strict --fail-on-missing"
          # Latter on we might be less conservative and accept some errors in case of scheduled updates (error mgt policy to be defined)
          python ./scripts/update_projects.py ./main-list/mapping.json ./main-list/projects.json -o ./main-list/projects-generated.json $OPTS
          diff ./main-list/projects.json ./main-list/projects-generated.json || :

      - name: 3️⃣ Validate generated catalog
        run: |
          nix run nixpkgs#check-jsonschema -- \
            --schemafile ./main-list/projects-schema.json ./main-list/projects.json

      - name: 4️⃣ On push in main, commit generated catalog to main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add ./main-list/projects-generated.json
          git diff --cached --quiet || git commit -m "Regenerate project catalog via CI"
          git push          

      - name: 4️⃣ On schedule, commit generated catalog to bot branch and create PR
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Regenerate projects catalog via CI"
          add-paths: "main-list"
          branch: ci/generated-projects
          base: ${{ github.ref_name }}
          title: "Regenerate projects catalog via CI"
          body: "Automatic update of projects_generated.json based on latest Codemeta or source meta data. Please check and merge if appropriate."
